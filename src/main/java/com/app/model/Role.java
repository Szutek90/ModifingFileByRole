package com.app.model;

/**
 * The Role enum defines the roles that can be assigned to a user within the system.
 * It includes the following roles:
 * ADMINISTRATOR - Represents a user with administrative privileges.
 * USER - Represents a regular user with standard privileges.
 *
 */
public enum Role {
    ADMINISTRATOR,
    USER;
}
